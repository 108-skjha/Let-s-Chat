Simply add 2 things in backend folder .env file

               1   MONGODB_URI=""
               2   JWT_TOKEN=""

Then run:- npm i , in both backend and fronend folder which will update the project with dependency file.
Then run:- npm run dev, in both backend and fronend folder then it will connect to the server in backend port no 3001 and in frontend 5173 port no.
Then simply perform signup .
